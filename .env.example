# Spotify Developer App credentials.
# Create an app at https://developer.spotify.com/dashboard, then copy the
# Client ID, Client Secret, and a Redirect URI you've registered for the app.

SPOTIFY_CLIENT_ID=your-client-id
SPOTIFY_CLIENT_SECRET=your-client-secret
SPOTIFY_REDIRECT_URI=http://127.0.0.1:8888/callback

# Note: OAuth tokens (access token, refresh token, expiry) are managed
# automatically by `npm run auth` and stored in .spotify-tokens (gitignored,
# chmod 0600). You should not need to set them by hand.
